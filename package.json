{
  "name": "dsh-session-kanban",
  "version": "1.2.0",
  "description": "Session kanban column for the DeepSeek Harness (dsh) web UI: three scrollable lists (todo/doing/done), per-project module filtering, drag & drop, and a one-click installer that patches the stock dsh web bundles.",
  "license": "MIT",
  "keywords": [
    "deepseek-harness",
    "dsh",
    "kanban",
    "session",
    "plugin",
    "web-ui"
  ],
  "scripts": {
    "build:dist": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-dist.ps1"
  },
  "engines": {
    "node": ">=18"
  }
}
