
## 1.2.0 — 2026-08-14

- 卡片菜单重做：支持 **重命名 / 分叉 / 归档** 与模块选择；点外部或 Esc 关闭；模块选择可选、不再强制选择才能关闭。
- 修复：看板折叠后无法再次展开（新增收起态的浮动展开按钮；补回丢失的 kanbanCol 类映射）。
- 修复：新建会话按钮去重——移除侧栏外壳的重复按钮，保留看板内「＋新建会话」（新增 ui-sidebar 补丁基线）。
- README 改为中英双语并加入 yulan 图片。# Changelog

## 1.1.0 — 2026-08-14

- **卡片两行排版**：会话名称独占一整行（超出省略），第二行承载模块徽标 / 相对时间 / 拖拽柄 / 菜单。
- **拖拽流畅化**：放下提示条改为常驻挂载（仅切换样式，不再增删 DOM，消除重排抖动）；跨列表高亮改为共享“当前悬停列”状态（取最后一次 dragover，rAF 节流）；三个列表 `react.memo` 化 + 回调 `useCallback` 稳定化 + 分组结果 `useMemo`。
- **修复 TDZ 渲染崩溃**：`useCallback(..., [mutate, current])` 在 `const current` 声明之前求值触发暂时性死区，导致看板整列渲染失败——派生值前移，并新增服务端渲染测试（`scripts/render-smoke.cjs`）防回归。
- 新增 `verify.ps1` 自检脚本。

## 1.0.0 — 2026-08-14

- 布局外壳四栏化：`项目列表 | 看板 | 对话 | 详情`（看板列在项目列表右侧，480–760px 可调宽、可折叠、窄屏自动隐藏）。
- 项目列表改造：一级=项目，二级=模块复选框（多选 / 全选 / 新建模块），单项目切换看板，项目列表不再平铺会话。
- 看板插件：三列等宽各自滚动（待执行 / 执行中 / 已完毕），点击卡片打开会话、卡片跨列拖拽、看板内新建会话、模块徽标、打开高亮、拖拽幽灵图与目标列提示。
- 一键安装 / 卸载 / 重启脚本与补丁基线（针对 `@deepseek-ai/dsh` 0.1.0-rc.6）。
